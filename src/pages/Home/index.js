import React from "react"

import SearchForm from "../../components/SearchForm"

import Styled from "./styled"

const Home = () => {
  return (
    <Styled.Layout>
      <SearchForm />
    </Styled.Layout>
  )
}

export default Home
